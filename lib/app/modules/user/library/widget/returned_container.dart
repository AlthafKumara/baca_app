import 'package:flutter/material.dart';

class ReturnedContainer extends StatelessWidget {
  const ReturnedContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}